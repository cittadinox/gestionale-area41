services:
  gestionale-area41:
    build: .
    container_name: gestionale-area41
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
    volumes:
      - gestionale_area41_data:/data
    restart: unless-stopped

volumes:
  gestionale_area41_data: